# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo

# Dependency dirs (none expected for static site, defensive)
node_modules/

# Logs & temp
*.log
*.tmp

# Local env
.env
.env.local

# Discord bot secrets + runtime files (live in repo root, never commit)
.discord_bot.env
.anthropic_api_key
.bot.heartbeat
.supervisor.heartbeat
.supervisor.lock
bot.log

# Python build artifacts (from tools/)
__pycache__/
*.pyc

# Large source files (raw photos / videos) — keep out of git
*.RAW
*.CR2
*.CR3
*.ARW
*.NEF
*.DNG

# Unrelated files that happen to live in the working folder
Dallas*.stl
*.stl

# Client job data and bulk media living alongside the site in
# C:\Project Claude\LF Propery Media. These are the working library, not
# site content -- keep every one of them out of git and off the deploy.
Filing System/
FLPM Drive/
FLPM Drive.zip
Pitch decks/
Raw Bracketed insta 360 pics/
drive-download-*.zip
*.mp4
*.mov
*.insp
*.insv

# Loose downloads that accumulate in the project root
Screenshot *.png
hcrgI.jpg
6869*.jpg

# tours/ is PUBLISHED site content, so unlike the job folders it has to be
# committed -- Cloudflare Pages deploys what is in the repo. These are the
# web-sized renders (4096x2048, ~1MB each), not the masters; the full-resolution
# originals stay in Filing System/ and remain ignored.

# Wrangler local state: local D1 databases, caches, build output.
.wrangler/
